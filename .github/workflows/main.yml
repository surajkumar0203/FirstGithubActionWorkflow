name: Suraj Kumar

# Event
on:
  schedule:
    - cron: "0 0 * * *"
    #   branches: [ master ]


# Job
jobs:
  demo:  # name of the job
    # runner
    runs-on: ubuntu-latest

    # steps: What tasks
    steps:
      - name: Greetings
        run: echo "Hello Suraj"

